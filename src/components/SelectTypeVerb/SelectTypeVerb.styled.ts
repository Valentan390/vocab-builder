import styled from "styled-components";

export const StyledSelectTypeVerbError = styled.p`
  position: absolute;
  left: 0;
  bottom: -7px;
  font-size: 12px;
  // font-style: normal;
  font-weight: var(--regular);
  line-height: 1.5;
  letter-spacing: 0.12px;
  color: var(--red);
`;
